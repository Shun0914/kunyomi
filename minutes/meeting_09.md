## **会議概要**

### **全体の進捗**

* **17チケット中7完了（約41%）**  
* 現状のペースは良好  
* 基本機能（登録・取得・検索・ジャンル取得）が見えてきており、**今週内に土台を固め、来週はメイン機能の実装に移行**する方針

---

## **バックエンドステータス**

### **機能実装・検証**

#### **なおみん担当API**

* **ローカルデータ投入・動作検証済み**  
* 検索は大文字検索でも登録済みの正規化された値を返却するなど、要件通り  
* 担当2件完了・検証済み  
* MySQL接続エラーはAI支援で解消  
* チケット\#15（詳細画面）を担当、\#17は未アサインでしゅんすけが判断予定  
* よこちのPRレビューを担当予定

#### **くぼしー担当API**

* **ジャンル関連APIを3本実装**  
* **課題：** 1本目と3本目でincludeInactive（インアクティブフラグの含め方）が効いていないため修正が必要  
* 3本必要かの見直し提起あり → **現状は用途に応じて維持の方向**（階層構造表示用とフラット取得用を使い分け）

---

### **API/データモデル方針（合意事項）**

#### **ジャンルAPI設計**

**サイドバー用：**

* 階層構造APIを継続利用

**ドキュメント作成時のジャンル選択：**

* ドロップダウンは**末端（レベル3）のみを表示・選択可**にする  
* 親ジャンル（上位階層）は選択不可  
* フロントでバリデーションを実装  
* APIは全件返してもよいが、UIはレベル3でフィルタして表示

**表示名称：**

* パス列の日本語化で頑張るより、基本は**末端ノードのnameをそのまま表示**する方針  
* 階層はデータ正規化上の構造として保持

#### **今後の検証**

* ドキュメント登録でのジャンル割当はレベル3限定で運用  
* 一覧・検索では必要に応じ階層情報を辿る

---

## **フロントエンドステータス**

### **進捗・見通し**

#### **くぼしー**

* **ジャンルセレクター（\#13）に着手・概ね動作**（約2時間で接続/表示まで）  
* 作成画面のAPI接続（\#16）は画面レイアウト含め重めのタスク見込み  
* フィグマ/既存コードの転用＋生成AI支援で実装を加速中  
* UI仕様で迷いがちな箇所は**一旦決め打ちで実装し、後で調整**する方針

#### **よこち**

* ドキュメント一覧取得APIを実装・ローカル検証済み  
* なおみんのMain反映取り込み・コンフリクト解消後にPR予定  
* フロント未着手だが本日～明日で着手し難易度を把握  
* レビュー用テストデータ（ユーザー/ドキュメント）投入スクリプトを追加予定

---

### **実装・レビューの工夫**

* 対応ページが未実装の機能は、**暫定のテスト画面を用意して検証可能にし、統合後に削除**する  
* 「API接続」チケットでも、実質的に関連ページの作成を含む前提で進める

---

## **プロセス＆ワークフロー**

### **PR/チケット運用**

#### **基本原則**

* **1チケット＝1PRを原則**  
* PR本文に\*\*「Close \#\<Issue番号\>」\*\*を記載して自動クローズを有効化  
* 仕様レビューはIssue/Backlogで管理し、PRは「**安全にMainへ統合できる動くもの**」を目的にする

#### **現状の課題**

* くぼしーのPRで複数チケットの変更が混在  
* **対応：** PR分割・不要コミットの削除・コンフリクト解消で整理予定

---

### **カンバン自動化**

* **最初はReady**  
* コミットで**In Progress**  
* PRマージで**Done**に自動遷移  
* 手動操作は不要

---

### **レビュー手順**

* レビュワーは**ブランチをローカルに取り込み、実データで動作確認**  
* フロント未連携箇所は暫定テスト画面で動作検証

---

## **テストとテストデータ**

### **テストデータ共有**

* 初期投入スクリプト（既存：ジャンル）に加え、**ユーザー・ドキュメントの投入スクリプトを追加予定**  
* 全員が同一データで検証できる体制に

### **E2E計画**

* 基本機能の統合後、テストケースをドキュメント化・共有  
* **平日（水/木目安）に各自ローカルでE2E検証**  
  * Mainブランチでの協調動作・データ連携の確認

---

## **展開および次のフェーズ計画**

### **デプロイ**

* **1周目の基本機能完了タイミングからAzureへのデプロイ準備を開始**  
* GitHub Actions想定  
* 想定外の詰まりに備え前倒しで着手  
* **担当：しゅんすけ**

---

### **次フェーズ（2周目以降）**

#### **優先機能**

* プロダクトのメイン機能である\*\*「樹形図」実装の技術調査→チケット起票\*\*（水曜までにしゅんすけ）  
* 差別化要素（全体マップ/QA/評価）も計画に組み込む  
* 通知は追加機能扱い

#### **低優先度**

* 認証・履歴・管理系は優先度低  
* 最終週はブラッシュアップ／バックログ消化に充当

---

## **決定事項**

### **ジャンルAPI**

1. 階層構造API（サイドバー用）とフラットAPI（選択用）は当面維持  
2. ドロップダウンは**レベル3（末端）のみ表示・選択可**  
3. 親は選択不可（フロントでバリデーション）  
4. UIは原則「末端nameの単独表示」  
5. パス列の日本語化は優先度を下げる

---

### **実装運用**

1. 仕様で迷う箇所は**決め打ちで実装し、後で見直す**（アジャイル運用）  
2. **PRは1チケット1PR**  
3. 仕様議論はIssue/Backlogで管理  
4. カンバンはコミット/マージで自動ステータス遷移  
5. フロント検証用の暫定テスト画面は許容（統合後に削除）

---

### **テスト・データ**

1. 共通初期データ投入スクリプトを整備・共有  
2. E2Eテストは水～木を目安に全員で実施（テストケースはしゅんすけが用意）

---

## **リスクと懸念**

### **1\. includeInactiveのフィルタ不備**

* くぼしーAPI 1本目/3本目で修正必要

### **2\. PRの混在・コンフリクト**

* 早期に分割/解消し、Mainの安定性を担保

### **3\. よこちのフロント未着手による見積り不確実性**

* 今週中の着手で感触を得る

### **4\. デプロイの詰まりリスク**

* 前倒しで準備開始し、問題切り分けを容易にする

---

## **対応事項**

* **@くぼしー：** includeInactiveフラグの不具合（1本目/3本目）を修正し、再度PR提出  
* **@くぼしー：** 混在したPRを分割し、不要コミットを整理のうえコンフリクト解消  
* **@くぼしー：** フロントのジャンルセレクターでレベル3のみ表示・選択可の実装を反映（暫定テスト画面は統合後に削除）  
* **@よこち：** ドキュメント一覧取得APIのPRを、なおみんのMain反映取り込み・コンフリクト解消後に提出  
* **@よこち：** ユーザー/ドキュメント初期データ投入スクリプトを作成し、別PRで提出（全員の検証用）  
* **@なおみん：** よこちのPRをレビュー（動作観点を含む）  
* **@しゅんすけ：** E2E用のテストケースを作成し、ドキュメントにアップロード  
* **@しゅんすけ：** 次フェーズ（樹形図等）の技術調査・チケット起票（水曜まで）  
* **@しゅんすけ：** 基本機能完了後、Azure/GitHub Actionsでのデプロイ準備を開始  
* **@全員：** 水～木を目安にMainブランチでE2E検証を実施（共有テストケース/初期データを使用）  
* **@よこち：** 要件定義書の提出状況を確認し、未提出者は提出する